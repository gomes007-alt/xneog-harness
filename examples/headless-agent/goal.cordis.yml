# Add the persisted goal domain and its model-facing tools to the real one-shot app.
- id: base
  name: '@xneog/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - insert:
          - id: goal
            name: '@xneog/dsh-goal'
          - id: tool-goal
            name: '@xneog/dsh-tool-goal'
